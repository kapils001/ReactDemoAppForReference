import React from "react";

const UserProfile = () => {
  return (
    <div>
      <h1>Welcome Bro!!</h1>
      <button className="btn btn-danger">Logout</button>
    </div>
  );
};

export default UserProfile;
